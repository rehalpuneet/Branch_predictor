Instruction to run the code in terminal or command line

Step 1: g++ branchsimulator_skeleton.cpp

Step 2: ./a.out config.txt trace.txt

Check the results in trace.txt.out



Makefile instructions:

just type the make command



In both the cases the number of miss will be shown in the terminal itself. 
The pdf of the graph has been added within the folder.
To see the number of misses for different values of m, change the value of m in config.txt file